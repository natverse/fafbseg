% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/l2cache.R
\name{flywire_l2attributes}
\alias{flywire_l2attributes}
\title{Query FlyWire level 2 cache attributes}
\usage{
flywire_l2attributes(
  rootid = NULL,
  l2ids = NULL,
  attributes = NULL,
  split_columns = TRUE,
  rval = c("data.frame", "list"),
  datastack_name = getOption("fafbseg.cave.datastack_name", "flywire_fafb_production"),
  ...
)
}
\arguments{
\item{rootid}{One or more FlyWire root ids defining a segment. Supply either
\code{rootid} or \code{l2ids}.}

\item{l2ids}{Optional vector of level 2 ids to query directly. Supply either
\code{rootid} or \code{l2ids}.}

\item{attributes}{Optional character vector of attribute names to retrieve.
\code{NULL} requests all available attributes.}

\item{split_columns}{Whether to expand multivalued attributes into separate
columns using the same names as python \code{caveclient}.}

\item{rval}{Return either a \code{data.frame} or the raw named \code{list}
returned by python \code{caveclient}.}

\item{datastack_name}{defaults to the value selected by
\code{\link{choose_segmentation}} and to "flywire_fafb_production" when
that is missing. See \url{https://global.daf-apis.com/info/} for other
options.}

\item{...}{Additional arguments passed to \code{\link{flywire_l2ids}} and to
recursive calls when \code{rootid} contains multiple ids.}
}
\value{
When \code{rval="data.frame"}, a tibble with one row per queried
  level 2 id and an explicit \code{l2_id} column stored as a 64 bit integer.
  When \code{rval="list"}, the raw named list returned by
  \code{caveclient$l2cache$get_l2data()}. When \code{rootid} has length
  greater than 1, a named list is returned with one result per root id.
}
\description{
Query FlyWire level 2 cache attributes
}
\examples{
\dontrun{
x <- flywire_l2attributes(
  rootid = "720575940604351334",
  attributes = c("size_nm3", "rep_coord_nm")
)
head(x)
}
}
